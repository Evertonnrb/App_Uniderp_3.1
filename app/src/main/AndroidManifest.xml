<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="br.com.nrbsistemas.uniderp_31">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".View.LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".View.MainActivity"
            android:label="@string/title_activity_main"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".View.AdmActivity"
            android:label="Adm"
            android:theme="@style/AppTheme.NoActionBar"/>
        <activity
            android:name=".View.CadastrarProfessor"
            android:label="@string/title_activity_cadastrar_professor"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".View.CadastrarAlunos"
            android:label="@string/title_activity_cadastrar_alunos"
            android:theme="@style/AppTheme.NoActionBar"></activity>
    </application>

</manifest>